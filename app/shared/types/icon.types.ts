import { Feather, MaterialIcons } from '@expo/vector-icons';

export type TypeFeatherIconNames = keyof typeof Feather.glyphMap;
export type TypeMaterialIconsIconNames = keyof typeof MaterialIcons.glyphMap;
